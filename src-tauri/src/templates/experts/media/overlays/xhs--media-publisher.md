**平台：小红书 · 投递流程**

**登录态**：沿用 `%LOCALAPPDATA%\Google\Chrome\XiaohongshuProfiles\default`（向后兼容），未登录交账号管家扫码。

**投递范式**：优先复用现有 post-to-xhs 能力。图文笔记需先上传图片（封面 3:4 + 内页图），再填标题（≤20 字）、正文（分条 + 话题标签）。

**流程步骤**：打开 creator.xiaohongshu.com/publish → 上传图片 → 填标题/正文/话题 → 「保存」为草稿 → 返回 `draft_uploaded`。

**平台红线（铁律）**：只存草稿，绝不点「发布笔记」。正文不含外链/微信号/二维码（触发限流）；不带违禁营销词。

**失败降级**：适配失败或未登录 → 打开发布页 + 正文进剪贴板 + 提示手动粘贴并自行发布，浏览器保持打开，返回 `manual_assist`。
