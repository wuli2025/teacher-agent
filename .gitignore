node_modules
dist
dist-ssr
*.local

# 本机/部署密钥永不入库。仅提交无值模板。
.env
.env.*
!.env.example
!.env.server.example
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
src-tauri/target
src-tauri/gen
src-tauri/Cargo.lock

# Runtime outputs / local logs
.dev.log
output/
PolarisKB/output/
# 对话产物（运行时生成，落在工作文件夹/KB 根下的 conversations/，不该进仓库）
PolarisKB/conversations/
conversations/

# Claude workspace / temp files
.claude/
*.html
!index.html
!src-tauri/**/*.html

# 本地开发产物 / 截图 / 日志（不进仓库）
.shot-*.png
.dev-screenshot-*.png
.dev-server.log
.dev-run.log
/workspace/
PolarisKB/.polaris_seeded

# 本地工作/产物草稿目录（运行时生成，不进仓库）
/career-map-work/
/polaris-video-work/
/polaris-video-work-kbwiki/

# 生成的视频工作产物(大体积,勿提交)
/polaris-video-work-longoutput/
/polaris-video-work-zhiyuan/
/polaris-video-work-kbwiki/

# Docker 化本地产物（勿提交）
.claude-seed/
.docker-build.log
.docker-monitor.log
.docker-monitor-dry.log
docker-compose.override.yml

# UI 预览构建产物（并行会话生成,8GB+,勿提交）
src-tauri/target-ui-preview/

__pycache__/
*.pyc

# 本地运行日志 / 临时脚本草稿（运行时生成,勿提交）
_scratch/
src-tauri/*.log
_devrun.log
easytier-panic.log

# 本地 TLS 证书,勿入库
docker/relay/certs/

# 根目录临时产物(实验脚本/日志/截图/评测中间件),一律不入库; _collabtest/ 为可重放测试例外
/_*
!/_collabtest/
/.rustpull.log
/*.log

# GEO: local relay TLS certs (never commit private keys)
docker/relay/certs/


# 误装进来的 polaris-app 安装副本(Windows 二进制,非源码,勿入库)
llmwiki/Polaris/

# wrangler 的本地模拟存储(r2 object put 不加 --remote 时写这里,别当成真上传了)
.wrangler/

# mathkit 的可重生中间产物:公式 PNG 缓存、逐页实渲图、联系表。
# 跑一遍 decks/*.py + render_pptx.ps1 就能重出,没必要几十 MB 进库;
# 最终 pptx、报告、Manim 源码、情境图仍然入库。
mathkit/out/*/tex/
mathkit/out/*/preview/
mathkit/out/*/sheets/
mathkit/texkit/out/
**/__pycache__/
